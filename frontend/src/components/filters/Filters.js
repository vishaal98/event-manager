import React from "react";
import styles from "./filters.module.css";

function Filters() {
  return (
    <div
      //   className={mobile ? styles.mobileFilterContainer : styles.filterContainer}
      className={styles.filterContainer}
    ></div>
  );
}

export default Filters;
